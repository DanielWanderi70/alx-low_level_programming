#include "main.h"
/*
 * function changes lowercase to uppercase
 */
char *string_toupper(char *)
{
	int i;
	for (i = 0; char[i] != '\0'; i++)
	{
		if [char[i] >= 'a' && char[i] <= 'z')
		{
			char[i] = char[i] - 32;
		}
		}
	return (char);
}



