#include "main.h"
/*
 * function appends two strings
 */
char *_strcat(char *dest, char *src)
{
	int i, j;
	for (i = 0; dest[i] != '\0'; i++)
	{
		for (j = 0; scr[j] != '\0'; j++)
		{
			_putchar(i j);
		}
	}
}

